import * as React from 'react';
import AddBook from '../app/components/AddBook';

function AddBookScreen({ navigation }) {
  return <AddBook navigation={navigation} />;
};

export default AddBookScreen;